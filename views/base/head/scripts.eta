<!-- Netlify cms redirect -->
<% if(it?.page?.name==='home'){%>
<script>
      if (location.hash && (location.hash.includes('invite_token') || location.hash.includes('recovery'))) {
        // Guard to redirect
        location.href = `/cms/es-ec/app${location.hash}`;
      }
</script>
<% }%>